import './Controls.css';

const Controls = (props: PropsType) => {
    return (
        <div>
            <button id="plus">+</button>
            <button id="minus">-</button>
        </div>
    )
}

export default Controls;

// ------------------------------------

interface PropsType {}